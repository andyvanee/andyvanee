---
pageable: true
per_page: 9
---

<ul class="article-list">
<% page_articles.each_with_index do |article, i| %>
  <li>
    <h2><%= link_to article.title, article %></h2>
    <span class="date"><%= article.date.strftime('%b %d, %Y')%></span>
    <div class="summary">
      <% if article.body.length > summary_length %>
        <%= article.summary %>
        <p class="read-more"><%= link_to "read article", article %></p>
      <% else %>
        <%= article.body %>
      <% end %>
    </div>
  </li>
<% end %>
</ul>

<% if paginate %>
  <div class="pagination">
    <% if num_pages > 1 %>
      <% if prev_page %>
        <span class="prev"><%= link_to '&laquo; Newer', prev_page %></span>
      <% end %>
      <span class="page-no">Page <%= page_number %> of <%= num_pages %></span>
    <% end %>

    <% if next_page %>
      <span class="next"><%= link_to 'Older &raquo;', next_page %></span>
    <% end %>
  </div>
<% end %>
